<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="mprog.nl.mars_weather_explorer.WeatherDataActivity">

    <item
        android:id="@+id/action_home"
        android:icon="@drawable/ic_home"
        android:title="Home"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_photo_load"
        android:icon="@drawable/ic_photo_camera"
        android:title="Load a different photo"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_date_picker"
        android:icon="@drawable/ic_date_picker"
        android:title="Pick a date"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="never" />
</menu>
